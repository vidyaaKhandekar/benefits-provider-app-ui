import { FC } from "react";
import { Text } from "@chakra-ui/react";
import Layout from "../components/layout/Layout";
const Home: FC = () => (
  <Layout>
    <Text as="p">This is the home page</Text>
  </Layout>
);

export default Home;
